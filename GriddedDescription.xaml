<UserControl x:Class="SolyankaGuide.GriddedDescription"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SolyankaGuide"
             mc:Ignorable="d" 
             VerticalAlignment="Top"
             Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=WrapPanel}, Converter={StaticResource ThreeColumnConverter}}"
             Height="{Binding Width, RelativeSource={RelativeSource Self}, Converter={StaticResource AspectRatioConverter}}">
    <Border Background="#99404040" CornerRadius="5" Padding="10, 10, 10, 5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Image x:Name="Image" Grid.Row="0" Source="/Internals/Images/placeholder.png" HorizontalAlignment="Stretch" VerticalAlignment="Top" Stretch="Fill"/>
            <TextBlock x:Name="TileName" TextWrapping="Wrap" Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" TextAlignment="Center" FontFamily="Segoe UI, Noto Sans, Arial, Times New Roman" FontSize="20" FontWeight="Bold" Foreground="Yellow" Background="#00000000"/>
        </Grid>
    </Border>
</UserControl>
